{% elif site.mode == 'django' %}

    Alias {{ django_url_static }} {{ django_root_static }}
    Alias /robots.txt {{ django_root_static }}/robots.txt
    LoadModule wsgi_module /usr/lib/apache2/modules/mod_wsgi.so

    <Directory {{ django_root }}/{{ django_base }}>
        <Files wsgi.py>
            Require all granted
        </Files>
    </Directory>

    <Directory "{{ django_root }}" >
        AllowOverride All
    </Directory>

    WSGIProcessGroup {{ apache_site }}
    WSGIDaemonProcess {{ apache_site }} python-home={{ django_root_venv }} python-path={{ django_root }}:{{ django_root_venv }}/lib/{{ django_venv_python }}/site-packages{% for py_path in django_pythonpaths %}:{{ py_path }}{% endfor %}

    WSGIScriptAlias / {{ django_root }}/{{ django_base }}/wsgi.py process-group={{ apache_site }} application-group=%{GLOBAL}